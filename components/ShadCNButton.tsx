import React from "react";
import { Button } from "./../srclib/components/ui/button";

const ShadCNButton = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <Button variant="default" size="lg">
        ShadCN Button
      </Button>
    </div>
  );
};
           
export default ShadCNButton;
